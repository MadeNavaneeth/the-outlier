# CloseLoop has ZERO runtime dependencies -- the standard library is enough.
#
#   python3 closeloop.py generate --out sample
#   python3 closeloop.py run
#   python3 closeloop.py serve
#
# all work on a bare Python 3.11+ install. Nothing below is required to run,
# demo or evaluate the project.
#
# The only thing you may want is the test runner:
pytest>=7.0        # python3 -m pytest -q
